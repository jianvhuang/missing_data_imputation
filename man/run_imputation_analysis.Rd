% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{run_imputation_analysis}
\alias{run_imputation_analysis}
\title{Run Imputation Analysis}
\usage{
run_imputation_analysis(
  data,
  continuous_vars,
  ordinal_vars,
  nominal_vars,
  info_vars = NULL,
  dir_output,
  dir_sourcecode = NULL,
  data_file_name = "data",
  output_file_name = "imputation_report.pdf",
  list_noNA = c(0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5),
  seed = 123,
  niter = 10,
  ntree = 100,
  maxiter = 10,
  k_values = c(3, 5, 7),
  mice_m = 5,
  mice_maxit = 10,
  micer_num_trees = 100,
  user_defined_features = NULL
)
}
\arguments{
\item{data}{A data frame to analyze}

\item{continuous_vars}{Vector of continuous variable names}

\item{ordinal_vars}{Vector of ordinal variable names}

\item{nominal_vars}{Vector of nominal variable names}

\item{info_vars}{Vector of identifier variable names (default: NULL)}

\item{dir_output}{Directory where output files will be saved}

\item{dir_sourcecode}{Directory containing source code files}

\item{data_file_name}{Name of the dataset for the report (default: "data")}

\item{output_file_name}{Name for the output report file (default: "imputation_report.pdf")}

\item{list_noNA}{Vector of missing rates to test (default: 0.05 to 0.5)}

\item{seed}{Random seed for reproducibility (default: 123)}

\item{niter}{Number of iterations (default: 10)}

\item{ntree}{Number of trees for Random Forest (default: 100)}

\item{maxiter}{Maximum iterations for Random Forest (default: 10)}

\item{k_values}{Vector of K values for KNN (default: c(3, 5, 7))}

\item{mice_m}{Number of imputations for MICE (default: 5)}

\item{mice_maxit}{Maximum iterations for MICE (default: 10)}

\item{micer_num_trees}{Number of trees for miceRanger (default: 100)}

\item{user_defined_features}{Additional features to include in imputation (default: NULL)}
}
\value{
A list containing imputation results
}
\description{
Performs a complete imputation analysis workflow on a dataset,
including data preparation, missing value analysis, imputation method comparison,
and report generation.
}
\details{
This function executes a complete imputation analysis workflow:
\itemize{
\item Prepares the dataset with proper variable types
\item Performs missing value analysis
\item Analyzes correlations between variables
\item Compares multiple imputation methods (Random Forest, KNN, MICE, miceRanger)
\item Selects the best-performing method
\item Applies the selected method to the dataset
\item Generates a comprehensive report
}
}
\examples{
\dontrun{
# Load example data
data(mtcars)

# Add some missing values
set.seed(123)
mtcars_missing <- mtcars
mtcars_missing[sample(1:nrow(mtcars) * ncol(mtcars), 30)] <- NA

# Run imputation analysis
result <- run_imputation_analysis(
  data = mtcars_missing,
  continuous_vars = c("mpg", "disp", "hp", "drat", "wt", "qsec"),
  ordinal_vars = c("cyl", "gear", "carb"),
  nominal_vars = c("vs", "am"),
  dir_output = "imputation_results/",
  dir_sourcecode = "code/",
  data_file_name = "mtcars"
)
}

}
